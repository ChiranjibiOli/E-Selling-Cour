# Landing components

This room owns the public landing-page components used to render live catalog content.

- `CourseCard.php` renders a published course card.
- `CategoryPill.php` renders an active category link.

Components in this directory receive prepared view data and must escape user-controlled output before rendering HTML.
