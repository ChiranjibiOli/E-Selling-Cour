# Backend feature room

This feature owns route, controller, middleware, request, validator, policy, handler, repository, response, event, and tests. Domain ownership is declared in `services/features.manifest.php`.
